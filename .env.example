M1_APP_ENV=local
M1_HTTP_PORT=3000

# Local development/test only. Leave blank until a local empty database is initialized.
# Do not commit real credentials, production hosts, real bills, master data, or ops confirmation outputs.
M1_DATABASE_URL=
M1_DATABASE_READONLY_URL=
M1_DATABASE_BACKGROUND_URL=
