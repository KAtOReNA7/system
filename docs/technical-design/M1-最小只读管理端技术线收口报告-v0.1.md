# M1 最小只读管理端技术线收口报告 v0.1

状态：CLOSED

日期：2026-06-21

## 1. 收口范围

本报告收口范围为 M1 数据基础阶段的最小只读管理端工程闭环。

收口对象包括：

- API 契约；
- 错误码归一化；
- `/admin` 最小只读管理端页面；
- 页面专业设计审计；
- 状态表达与文案归一化；
- E2E 自动化测试；
- GitHub Actions CI 门禁；
- 安全边界。

本报告不包含：

- 正式数据迁移；
- 真实账单导入；
- 数字版权台账导入；
- 运营确认结果导入；
- mapping_version 激活；
- 导入、激活、撤销、重试、取消等写操作；
- 正式库连接。

## 2. 已完成工程项

### 2.1 API 契约

已完成 M1 最小只读 API 契约整理：

- `GET /health`
- `GET /health/db`
- `GET /api/system/status`
- `GET /api/works`
- `GET /api/works/:id`
- `GET /api/mapping-versions`
- `GET /api/mapping-versions/:id`
- `GET /api/jobs`
- `GET /api/jobs/:id`

相关文档：

- `docs/api/M1-api-contract-v0.1.md`
- `docs/api/M1-openapi-v0.1.yaml`

### 2.2 错误码归一化

已完成统一错误响应边界：

- `database_not_configured`
- `database_unavailable`
- `bad_request`
- `not_found`
- `internal_error`

错误响应保留技术 `code` 与 `requestId`，不暴露连接串、密码、SQL、堆栈、主机端口或真实业务数据。

### 2.3 最小只读管理端页面

已实现入口：

- `GET /admin`
- `GET /admin/`
- `GET /admin/app.css`
- `GET /admin/app.js`

页面区域：

- 系统状态；
- 作品列表；
- 映射版本；
- 后台任务。

页面仅调用当前只读 API 或使用前端内存合成 fixture，不提供写操作入口。

### 2.4 设计审计与文案归一化

已完成：

- 专业设计审计；
- 空库与未配置数据库文案区分；
- 生命周期不可读状态卡片；
- degraded / empty / error / not found 状态说明；
- 常见技术状态中文展示；
- 作品列表、映射版本、后台任务说明；
- 移动端表格横向滚动提示。

### 2.5 E2E 自动化测试

已完成最小浏览器 E2E 测试。

覆盖：

- `/admin` 默认无数据库配置 degraded 态；
- fixture 模式下系统状态、作品列表、映射版本、后台任务 success 态；
- 390px 移动端；
- 不存在写操作入口。

E2E 使用：

- Node 内置 `node:test`；
- Playwright Chromium；
- 进程内本地 HTTP 服务；
- 无数据库 URL；
- 前端内存合成 fixture。

## 3. CI 远端证据

GitHub Actions run：

| 字段 | 值 |
| --- | --- |
| run id | `27901183952` |
| workflow name | `CI` |
| branch | `main` |
| headSha | `35e49c67a205d26fd3cf589d7c349e2c6626ebfc` |
| status | `completed` |
| conclusion | `success` |
| URL | `https://github.com/KAtOReNA7/system/actions/runs/27901183952` |

Job：

| job id | name | status | conclusion |
| --- | --- | --- | --- |
| `82561660990` | `verify` | `completed` | `success` |

通过的关键步骤：

- Install Playwright Chromium；
- Check for real data and secrets；
- Lint；
- Build；
- Test；
- Smoke；
- Admin E2E。

该 run 的 `headSha` 与目标提交完全一致：

```text
35e49c67a205d26fd3cf589d7c349e2c6626ebfc
```

## 4. 本地验证

此前完整本地验证已通过：

```text
npm run lint
npm run build
npm test
npm run smoke
npm run check:no-real-data
npm run test:e2e
```

本轮仅更新报告，执行：

```text
npm run check:no-real-data
```

结果：通过。

## 5. 安全边界确认

本阶段仍保持以下边界：

- 未连接正式数据库；
- 未写正式数据库；
- 未使用正式库凭据；
- 未读取或导入真实账单；
- 未导入数字版权台账；
- 未导入运营确认 Excel 或运营确认结果；
- 未新增写接口；
- 未新增导入、激活、撤销、重试、取消 UI；
- 未激活 `mapping_version`；
- 未调用 `switch_mapping_version`；
- 未执行正式数据迁移；
- 未修改 `db/migrations/`；
- 未修改 Flyway 历史迁移；
- 未读取 `data/` 作为应用启动输入；
- 未使用 `git add .`；
- 未清理、应用或删除 stash。

## 6. 最终收口结论

M1 最小只读管理端技术线已完成阶段性工程闭环。

闭环依据：

- 只读 API 已实现；
- API 契约已整理；
- 错误码已归一化；
- `/admin` 最小只读页面已实现；
- 设计审计与文案归一化已完成；
- E2E 自动化测试已完成；
- GitHub Actions CI 门禁已接入；
- 目标提交远端 CI run `27901183952` 已完成并通过；
- 安全边界未越界。

允许进入下一阶段：

```text
M1 管理端后续只读体验完善或下一业务模块设计
```

仍禁止进入：

```text
正式数据迁移
真实账单导入
数字版权台账导入
运营确认结果自动应用
mapping_version 激活
正式库连接
写操作管理端
```
