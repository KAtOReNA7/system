# M2 出版行业规模适配 threshold impact map v1

本图谱绑定出版行业适配的渠道月度发生—条件金额核心
（Publishing-Scale Channel Monthly Occurrence × Conditional Amount Core，
`M2-CHAN-PSC01`，`M2-EXP-PUBLISHING-SCALE-CHANNEL-01/CORE`）与
`M2-PUBLISHING-SCALE-SUPPORT-01`。

## 闭环结论

- PRD、评价合同、支持合同与 active runtime config 指向同一新模型身份。
- 新 canonical core 没有把历史 `50/100` 或月度行数当作当前通用训练资格。
- 公开 diagnostic 为每层展示支持等级、distinct/positive works、work-cluster ESS、
  现金 ESS、集中度、连续收缩权重和回退原因。
- Model Registry 与通用 query 命令可以解释新模型和完整实验臂身份。
- scoped `AGENTS.md` 明确禁止以后重新引入无本项目证据的通用固定门槛。
- 历史 `M2-CHAN-GEN02` 配置、预注册、评分、阻断和 verifier 保留不变。
- K7C 没有读取新候选 outer outcome；Linux/Windows 与 clean-clone 门禁必须在
  exact HEAD CI 后才从 pending 转为通过，之后才可解锁一次 K7D 私有执行。

机器可核验明细见
`docs/analysis/m2-current/M2-publishing-scale-threshold-impact-map-v1.json`。
