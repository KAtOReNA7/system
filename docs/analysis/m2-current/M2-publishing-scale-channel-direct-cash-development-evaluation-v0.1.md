# 出版行业渠道直接现金尺度条件金额模型 v0.1：开发评价

模型稳定 ID：`M2-CHAN-PSC03`；原始候选 ID：`M2-CHAN-PSC03-RAW`；实验稳定 ID：
`M2-EXP-PUBLISHING-SCALE-CHANNEL-DIRECT-CASH-03`。

当前是结果前状态：唯一历史开发重放尚未执行
（`NOT_EXECUTED_PRE_EXECUTION_EXACT_HEAD_CI_REQUIRED`），没有模型性能证据
（`NO_MODEL_PERFORMANCE_EVIDENCE`）。当前文件不含 WAPE、FVA、bootstrap、门禁或模型
结论；公共实现与 synthetic 成功路径不能替代私有候选成绩。

只有预注册、全部公共门禁、全新 clone 验证和 pre-execution exact-head Linux/Windows CI
均成功后，才允许执行本任务授权的唯一一次开发重放（`DEVELOPMENT_REPLAY`）。

始终保持 `activeCandidate=null`、`approvedForAutomation=null`、
`productionReady=false`、`finalHoldoutOpened=false`、
`independentEvaluationOpened=false` 与 `laterOriginOpened=false`。
