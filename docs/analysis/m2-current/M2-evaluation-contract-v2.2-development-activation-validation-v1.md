# M2 评价合同 v2.2 开发激活验证 v1

## 验证结论

状态：
`M2_EVALUATION_V2_2_ACTIVE_FOR_DEVELOPMENT_WITH_DISCLOSED_RESIDUAL_EXCLUSION`。

该状态只在当前执行提交的 Linux 与 Windows 公共 CI 均成功后生效，且授权范围仅为
开发评价。

## 门禁结果

- 原始分成账单 190,663 行和 143 条冲销均保留；物理删除数为 0。
- 已追溯抵消为 `1228645.514699999995355150`；已分配部分没有删除。
- 隔离残差精确等于未分配残差：
  `-267.769000000000330000`，精确整数为
  `-267769000000000330000`。
- 四视图存在，开发可建模精确对账差为 0。
- forecast origin 后冲销进入该 origin 的行数为 0。
- 6 个冻结 artifact 共 716,801 行，摘要全部匹配；预测生成和预测修改均为 0。
- 残差导致的整案阻断为 0；292 个非组合 case 恢复开发标签。另有 30 个组合 cell
  因独立的人口成员不匹配而保持不可重评分，不归因于残差。
- 两次完整执行比较 7 个 private 输出文件，逐字节一致。
- 公共 artifact 仅含聚合，没有逐笔作品/渠道标识或 private 路径。

## 授权边界

v2.2 是开发评价合同，不是完整财务 final restatement、production 或 automation
gate；不改变 v2.1、上一版 v2.2 阻断证据或任何历史 raw failure，也不自动授予任何
模型晋升。
