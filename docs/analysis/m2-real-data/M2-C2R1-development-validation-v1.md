# M2 C2-R.1 development 验证 v1

## 结论

C2-R.1 结论为 **FAIL**，仍为 `not_for_formal_decision`。模型人口保持 7851 个 case；overall WAPE 为 0.5838，signed bias 为 +0.0293。13/23 项验收条件通过。

纯买断无承诺始终 raw/served null，未按 0 计分；final holdout、embargo shadow 与 deferred 60-month labels 均未打开。即使候选 PASS，也仍需中文业务抽检和用户明确批准；本轮在 C2-R.1 后停止。
