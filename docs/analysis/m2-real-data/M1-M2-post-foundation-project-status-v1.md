# M1/M2 基础字段收口后项目状态 v1

## 结论

- 用户最终分类与标签基础大表已完成校验并固定；当前约定范围内的人工基础字段已在本地文件级候选层收口。
- M1 本地数据 readiness 已具备重算条件，但尚未写正式主数据，也未执行完整正式验收，因此 M1 formal acceptance 仍未完成。
- M2 本地工程候选已包含收入模式、历史表现、生命周期、v1.1 conditional 预测、评级、风险/复核提示和回测证据；新基础表尚需进入一次 readiness 与评估一致性重算。
- 较早 M2 聚合仍使用 3054 部范围，而最终固定基础表为 3053 部；该 1 部范围差异必须由工程重算解释并统一，不能交给用户凭感觉处理。
- M2 formal completion 仍未完成，M3 formal execution 仍不可开始。

## 基础数据状态

| 数据项 | 本地候选状态 | 当前人工缺口 | Formal 状态 |
|---|---|---:|---|
| 作者 | 已收口 | 0 | 未写正式主数据 |
| 版权开始 | 已收口 | 0 | 未写正式主数据 |
| 版权到期 | 已收口；无限期为有效值 | 0 | 未写正式主数据 |
| 作品状态 | 已收口 | 0 | 未写正式主数据 |
| 音频版权状态 | 已收口 | 0 | 未写正式主数据 |
| 一级/二级/三级分类 | 用户最终基础表已固定 | 0 | 未写正式主数据 |
| 辅助标签 | 387 部、532 个标签赋值已固定 | 0 | 未写正式主数据 |
| 特殊属性标签 | 当前阶段不启用独立字段 | 0 | 后续新增规则时另行治理 |

分类与标签固定范围为 3053 部作品：出版物 1195 部、网文 1858 部。相对系统预填基线，用户修正 836 部作品。新增科普、教辅、诗歌三级分类及 11 个辅助标签已纳入受控词表。

## PRD 对齐状态

| PRD 能力 | 当前状态 | 结论 |
|---|---|---|
| 标准作品和基础字段 | 本地文件级候选完整 | 可重跑 M2 readiness；不等于正式主数据验收 |
| 历史收入与收入模式 | 本地真实数据候选已实现 | 需随最终基础表重算分层一致性 |
| 生命周期与评级 | rating-standard-v3 本地候选 | 非最终正式发布结果 |
| 剩余版权期预测 | v1.1 conditional | 聚合回测为 conditional pass，仍需业务接受与 formal gate |
| 风险与运营处理 | 风险/复核提示存在；自动建议主输出已移除 | 符合“无证据不强行建议”的当前边界 |
| 回测 | 有本地真实数据回测证据 | 需基于最终分类标签重跑分层统计 |
| formal task/export/release/audit | 未完成正式闭环 | 阻断 M2 formal completion |
| mapping activation | 未执行 | 阻断正式 release，不影响本地候选分析 |

## 当前仍需人工介入

1. **当前基础字段**：无需人工介入。2 个作者显示误操作已经恢复，分类和标签人工缺口为 0。
2. **业务复核**：旧汇总中的 142 个到期但仍有收入样本和 92 个版权有效但收入稀疏样本需先工程重算；重算后仍存在的项目再由业务判断。它们不是基础字段缺失。
3. **M2 候选接受**：最终基础表重算后，用户需要确认 v1.1 conditional 预测和 rating-standard-v3 是否继续作为有限业务复核基线。
4. **Formal 授权**：正式主数据写入、formal evaluation、正式 task/export/release/audit、mapping activation 和 M3 formal execution 均需分别明确授权。

## 无需用户即可继续的工程动作

1. 使用 `M2-classification-tag-foundation-local-fixed-cn-v1.json` 重跑 M2 readiness。
2. 重算分类/标签分层的预测、评级、风险和回测聚合，检查最终基础表是否引入回归。
3. 对齐旧 M2 3054 部聚合与最终基础表 3053 部范围，输出脱敏差异原因。
4. 更新 142/92 业务复核桶的脱敏汇总和易读复核包。
5. 输出新的 M2 formal gap 审计；在取得授权前不写正式主数据、不进入 M3 formal execution。

## 边界

- 本报告只包含聚合信息，不包含作品名、作者名、渠道名或行级收入明细。
- private 固定基础表和 JSON 不进入版本控制。
- 当前结论是本地候选层状态，不是生产发布审批结果。
