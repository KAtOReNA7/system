# M2 C2 高价值 B4 保护审计 v1

Top10 共有 838 个模型人口 case；所有 Top10 case 均经过冻结的 guard。guard 回退 B4 的 case 数为 306，通过 earlier-origin 证据保留非 B4 覆盖的 case 数为 0。

Top1、Top5、Top10 均按 cutoff 前 trailing-12 收入排名。当前 outer top-band actual 未用于选择，guard 规则未按结果修改。
