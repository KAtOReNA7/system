# M2 正式执行授权 checkpoint v1

## 授权结论

- 用户于 2026-07-13 明确授权 M2 正式主数据写入、正式基础信息版本/输入快照、mapping activation、formal evaluation 以及正式 task/export/release/audit。
- 授权已经成立，但不等于上述操作已经执行或 M2 已正式完成。
- M3 formal execution 未获授权；3 至 5 份代表性选题材料明确暂缓到 M2 两类复核桶和正式链路收口之后。

## 产品决定

- `REQ-M2-OUTPUT-001` 已冻结为 M2 不输出自动运营建议或资源投入动作。
- M2 只允许输出风险和事实型复核提示，不能替用户选择推广、续约、下架、定价或其他运营动作。
- 当前和后续正式导出均不得包含运营建议字段；历史 fixture/prototype 字段只保留回归追溯用途。

## 强制执行顺序

1. 用户完成 146 个到期仍有收入样本和 92 个版权有效但收入稀疏样本的确认。
2. 系统应用确认结果，并使逐作品 private 输入通过内容契约。
3. 创建正式基础信息版本和不可变输入快照。
4. 执行 dry-run、严格对账和回滚准备。
5. 写入正式主数据并激活已验证的 mapping version。
6. 执行 formal evaluation。
7. 执行正式 task/export/release/audit 闭环。

任何一步失败都必须停止后续步骤，不得把授权解释为可以绕过数据确认、对账、回滚或审计门禁。

## 环境边界

- 当前授权适用于项目已授权的本地环境和正式链路建设。
- 未授权连接远端生产、共享或 staging-like 数据库。
- 未授权提交 private 明细、原始账单、台账、环境文件、数据库 dump 或连接凭据。
