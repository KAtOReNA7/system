# M1 候选映射构建准备报告 v0.1

生成时间：2026-06-21T14:27:02+08:00

## 结论

- 源账单修正闭环：通过。
- 异常ID阻断：已清零。
- 候选映射预检：通过。
- 是否允许进入“请求用户授权生成正式候选 mapping_version”：是。
- 本轮仍禁止：写数据库、生成正式 mapping_version、自动应用候选映射、修改源账单、修改数字版权台账、修改 v2.3.1 工作簿、复制或修改 Flyway 正式迁移、导入真实数据到数据库。
- 正式数据迁移：仍禁止。

## 一、修正后源账单闭环校验

| 检查项 | 结果 |
| --- | --- |
| 源账单 SHA-256 | `b7ed5b08bcfde56d78d0df5fbe63a084c45467b520fd2dda6ce2f822c0d6ba94` |
| SHA-256 是否匹配预期 | 是 |
| 修正后账单数据行数 | 192872 |
| 数据行数是否匹配预期 | 是 |
| 较修正前减少行数 | 27 |
| 27 行目标记录是否已不存在 | 是 |
| 空“我方作品ID”行数 | 0 |
| `未匹配实销抹平` 行数 | 0 |
| `bill_abnormal_id_group_count` | 0 |
| `03-异常作品ID确认表`任务数 | 0 |
| 年月关键空值 | 0 |
| 作品ID关键空值 | 0 |
| 金额关键空值 | 23 |

说明：当前仍有 23 行实销金额为空；按现有真实账单分析口径，该项为 PENDING-DATA/未启用规则，不参与候选ID映射构建，但正式数据迁移或真实数据导入前仍需单独治理。

## 二、`ignored_paths_ok=false` 说明

- 具体来源：`tools/m1-master-data-analysis/validate_master_data_analysis.py` 中的路径治理检查要求 `.gitignore` 包含精确字符串 `/data/real-bills/`、`/data/master-data/`、`/data/m1-real-bills-private/`、`/data/m1-master-data-private/`。
- 当前 `.gitignore` 使用了 `data/`、`**/data/` 等泛化规则，实际私有数据文件被 `git check-ignore` 命中。
- Git 跟踪风险：未发现真实账单、数字版权台账、运营确认包或私有分析目录被 Git 跟踪。
- 是否影响账单数据正确性：否。
- 是否阻断候选映射构建准备：否。

该项不是账单业务数据异常，不影响候选映射构建；但需在Git/路径治理任务中单独修复或记录。

## 三、v2.3.1 运营确认结果对齐

| 检查项 | 结果 |
| --- | --- |
| v2.3.1 工作簿可打开 | 是 |
| 专项复核通过数 | 20 |
| 多授权分类已合并数 | 18 |
| 跨组冲突已合并数 | 2 |
| 源账单修正完成记录是否存在 | 是 |
| 账单登记个例是否已固化 | 是 |
| 多ID归并可进入候选构建数量 | 85 |
| 跨工作表冲突数量 | 0 |
| 修正后原 2 个异常ID任务是否仍进入待处理阻断 | 否 |
| 影响候选映射的未解决阻断 | 0 |

## 四、候选映射构建准备结果

| 指标 | 数量 |
| --- | ---: |
| 候选标准作品映射 | 344 |
| 候选历史分册映射 | 67 |
| 跨组专项归并映射 | 10 |
| 账单登记个例例外映射 | 2 |
| 个案例外数 | 1 |

### 预检项

| 检查项 | 结果 |
| --- | --- |
| 每条映射有来源任务ID | 是 |
| 每条映射有来源依据 | 是 |
| 同一完整原始ID映射多个有效主标准作品 | 0 |
| 同一标准作品同一业务形态多个常规原始ID | 1 |
| 候选映射引用已删除异常账单记录 | 0 |
| 候选映射依赖未解决任务 | 0 |
| 非法作品ID进入映射 | 0 |
| 授权分类反向覆盖业务形态 | 0 |
| `12345` 与 `Y12345` 被错误当成同一完整原始ID | 0 |
| 历史分册映射与常规映射未解决跨表冲突 | 0 |

补充说明：

- 标准映射与历史/跨组映射之间存在 41 个预览层级重叠；构建准备清单已声明优先级：跨组专项 > 历史分册 > 标准作品候选，因此有效映射冲突为 0。
- “同一标准作品同一业务形态多个常规原始ID”存在 1 组，来源于运营确认/专项复核，不是规则引擎自动推断；正式生成前应在人工确认摘要中继续保留。
- 账单登记个例例外仍只允许作为运营明确例外，不得让规则引擎自动学习或复用。

## 五、下一步门禁

- 可以进入：请求用户授权生成正式候选 mapping_version。
- 本轮没有生成正式 mapping_version。
- 即使下一步生成正式候选 mapping_version，也仍不得自动应用映射、不得写数据库。
- 正式数据迁移仍禁止；金额空值、最新月份完整性和路径治理等事项需在迁移前单独处理或形成明确放行记录。
